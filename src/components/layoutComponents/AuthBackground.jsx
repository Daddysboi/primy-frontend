import { Link } from "react-router-dom";

const AuthBackground = ({ children }) => {
  return (
    <>
      <p>
        If login and logout background are same, you can use one AuthBackground
        and pass as props
      </p>
    </>
  );
};

export default AuthBackground;
